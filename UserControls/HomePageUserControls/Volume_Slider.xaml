<classes:ControllerUserControl x:Class="Everything_Handhelds_Tool.UserControls.HomePageUserControls.Volume_Slider"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Everything_Handhelds_Tool.UserControls.HomePageUserControls"
             xmlns:classes="clr-namespace:Everything_Handhelds_Tool.Classes.Controller_Object_Classes"  xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                               mc:Ignorable="d" 
            Height="Auto"  Margin="0,0,0,3">
    <Border x:Name="border" Style="{StaticResource userControlBorder}" >
        <ui:CardExpander  VerticalAlignment="Top" x:Name="card"  ExpandDirection="Down" IsExpanded="True">
            <ui:CardExpander.Header >
                <DockPanel   >
                    <ui:SymbolIcon x:Name="statusIcon" Symbol="Speaker216" FontSize="30" Margin="0,0,8,0"  VerticalAlignment="Top"   />
                    <Viewbox Height="30" VerticalAlignment="Center"  HorizontalAlignment="Left" >
                        <TextBlock  Text="{DynamicResource Usercontrol_Volume}"  />
                    </Viewbox>
                    <ui:ToggleSwitch x:Name="toggleSwitch"  VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,20,0" DockPanel.Dock="Right" Checked="toggleSwitch_Checked" Unchecked="toggleSwitch_Unchecked"   />


                </DockPanel>
            </ui:CardExpander.Header>
            <DockPanel  Height="30" Margin="0,-10,0,0">
                <Slider  x:Name="control" Grid.Column="0" Interval="5" VerticalAlignment="Bottom" Minimum="0" Width="330" Maximum="100" HorizontalAlignment="Stretch"  IsSnapToTickEnabled="True" TickFrequency="1" Margin="5,-18,5,0" Thumb.DragCompleted="control_DragCompleted" Thumb.DragStarted="control_DragStarted" />

                <Viewbox   Grid.Column="1"   Margin="0,0,0,0" HorizontalAlignment="Right">

                    <TextBlock  Text="{Binding ElementName=control, Path=Value,StringFormat= '\{0\} %'}" />

                </Viewbox>



            </DockPanel>

        </ui:CardExpander>
    </Border>
   

</classes:ControllerUserControl>
