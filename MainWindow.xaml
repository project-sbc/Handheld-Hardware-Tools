<ui:UiWindow x:Class="Everything_Handhelds_Tool.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Everything_Handhelds_Tool"
       xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        
        WindowStyle="None"
        Title="Everything Handhelds Tool" 
        SizeToContent="Height"
        Width="500"
      
 Closing="Window_Closing"
        >
    <Grid >
            
        
        <Grid.RowDefinitions>

            <RowDefinition Height="45"  />
            <RowDefinition Height="40"  />
            <RowDefinition Height="*" />

            <RowDefinition Height="45" />
        </Grid.RowDefinitions>
        <ui:TitleBar ShowMaximize="False" ShowMinimize="False" CloseClicked="TitleBar_CloseClicked" >

        </ui:TitleBar>




        <Grid Grid.Row="1" Margin="6,0,6,0">
            <ui:SymbolIcon   FontFamily="Segoe MDL2 Assets"  HorizontalAlignment="Left">
                <ui:SymbolIcon.Style>
                    <Style>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ElementName=controllerStatusBarIcon, Path=Visibility}" Value="Collapsed">
                                <Setter Property="Button.Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ui:SymbolIcon.Style>
            </ui:SymbolIcon>
            <ListBox x:Name="navigationViewListBox" HorizontalAlignment="Center" VerticalAlignment="Center" Background="Transparent" >
                <ListBox.Resources>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Background" Value="Transparent" />
                        <Style.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="0"/>

                            </Style>
                        </Style.Resources>
                    </Style>
                </ListBox.Resources>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid  Height="25">
                            <ui:FontIcon FontFamily="Segoe MDL2 Assets" Glyph="{Binding}"/>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            <ui:SymbolIcon FontFamily="Segoe MDL2 Assets"  HorizontalAlignment="Right">
                <ui:SymbolIcon.Style>
                    <Style>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ElementName=controllerStatusBarIcon, Path=Visibility}" Value="Collapsed">
                                <Setter Property="Button.Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ui:SymbolIcon.Style>
            </ui:SymbolIcon>
        </Grid>

         
       
        <Frame x:Name="frame" Grid.Row="2" Source="Pages/HomePage.xaml" HorizontalAlignment="Stretch"   />

        <ui:Card Grid.Row="3" >
            <StackPanel Orientation="Horizontal" Grid.Row="3" Margin="6,-7,6,-4" >
                <Viewbox MaxHeight="25"  Margin="0,0,6,0" >
                    <TextBlock x:Name="timeStatusBar" FontSize="30" />
                </Viewbox>

                <ui:SymbolIcon x:Name="controllerStatusBarIcon"  Symbol="XboxController48" FontSize="23" Filled="True"  />
                <ui:SymbolIcon x:Name="wifiStatusBarIcon" Symbol="Wifi120" FontSize="23" Filled="True" Margin="6,0,0,4" Visibility="Visible" />
                <ui:SymbolIcon x:Name="noInternetStatusBarIcon" Symbol="WifiOff20" FontSize="23" Filled="True" Margin="6,0,0,4" Visibility="Visible" />
                <ui:SymbolIcon x:Name="chargingStatusBarIcon" Symbol="BatteryCharge20" FontSize="23" Filled="True" Margin="6,0,0,4" Visibility="Visible" />
                <ui:SymbolIcon x:Name="batteryStatusBarIcon" Symbol="Battery020" FontSize="23" Filled="False" Margin="6,0,0,4" Visibility="Visible" />

                <Viewbox MaxHeight="25" Margin="0,0,6,0" Name="viewBoxBatteryPercentage" >
                    <TextBlock x:Name="batteryPercentageStatusBar" FontSize="30"/>
                </Viewbox>

            </StackPanel>

        </ui:Card>


        <ui:InfoBar Grid.Row="2" IsOpen="True" Message="Hello" VerticalAlignment="Bottom" />

    </Grid>



</ui:UiWindow>
