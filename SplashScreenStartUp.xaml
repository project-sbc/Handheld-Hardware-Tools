<Window x:Class="Handheld_Hardware_Tools.SplashScreenStartUp"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Handheld_Hardware_Tools"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        xmlns:fa="http://schemas.fontawesome.io/icons/"
        Title="SplashScreenStartUp" Height="250" Width="400">
    <Border CornerRadius="20" >
        <Border.Background>
            <ImageBrush ImageSource="{DynamicResource SplashscreenLogo}"  Stretch="Fill"/>
        </Border.Background>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="4*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <fa:ImageAwesome Icon="Spinner" Spin="True" Height="38" Width="48" Grid.Row="1" Foreground="White" VerticalAlignment="Top" />
        </Grid>
    </Border>
</Window>
